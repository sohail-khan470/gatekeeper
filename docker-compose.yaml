version: '3.8'
services:
  redis:
    image: "redis:8.6"
    container_name: gatekeeper-redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/var/cache/data

volumes:
  redis_data:
    driver: local
